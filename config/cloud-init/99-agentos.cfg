#cloud-config
#
# AgentOS cloud-init configuration
# Runs the non-interactive setup wizard on first boot if setup.conf is present.
# Place your setup.conf at /etc/agentos/setup.conf before or during provisioning.
#
runcmd:
  - [/opt/agentos/bin/setup-wizard.sh]
